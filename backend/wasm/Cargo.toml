[package]
name = "ducia-wasm"
version = "0.1.0"
edition = "2024"
description = "WASM bindings for shared ducia logic (markdown, validation)"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
pulldown-cmark = "0.12"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
